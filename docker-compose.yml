services:
  database:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: foodmood
      POSTGRES_USER: foodmood
      POSTGRES_PASSWORD: foodmood
    ports:
      - "5432:5432"
    volumes:
      - foodmood-db:/var/lib/postgresql/data

volumes:
  foodmood-db:
